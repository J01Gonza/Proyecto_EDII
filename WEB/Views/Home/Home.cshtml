@model IEnumerable<WEB.Models.Chats>

@{
    ViewData["Title"] = "Home";
}

<h1>Chats</h1>

<div class="text-center">
   @if (!Model.Any())
    {
        <h3>No tienes ningún chat activo.<br>Inicia una conversación con tus <a asp-action="Contacts">contactos</a> o <a asp-action="AddFriend">agrégalos</a></h3>
    }
    else
    {
        //acceso a contactos
        //lista de chats y crear chat grúpal
    }

</div>
